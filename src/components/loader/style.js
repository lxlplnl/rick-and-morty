import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  container: {
    position: 'fixed',
    width: '100%',
    height: '100%',
    top: 0,
    left: 0,
  },
});
